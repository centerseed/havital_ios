# Paceriz Apple Watch App - 最終設計方案 ✅

> 基於用戶反饋的確認版本 - 2025-11-17

---

## 🎯 設計決策總結

| 問題 | 決定 |
|------|------|
| 訓練中指標顯示 | **緊湊型** - 單頁顯示所有信息 |
| 配速區間寬度 | **統一 ±20 秒** |
| 間歇/組合訓練 | **重點功能** - 分段追蹤 + 提示音 |
| 支持訓練類型 | **跑步相關** - Phase 1 專注跑步 |
| 心率區間命名 | **簡短名稱** + 多國語言支持 |
| 訓練完成行為 | **手動結束** → 總結頁 → 點擊完成返回 |
| 同步策略 | **可選自動同步**，iOS 主動發起 |
| 最低系統版本 | **watchOS 9.0+** |

---

## 📱 詳細 UI 設計

### 頁面 1: 課表列表

```
┌─────────────────────┐
│  本週課表  第 3/12 週  │
├─────────────────────┤
│ 今天 11/17          │
│ 🟢 輕鬆跑           │
│ 8.0 km | 5'30"/km  │
├─────────────────────┤
│ 明天 11/18          │
│ 🟠 間歇跑           │
│ 6×1000m | 4'00"/km │
├─────────────────────┤
│ 11/19              │
│ 🔵 長距離跑         │
│ 15.0 km | 5'45"/km │
└─────────────────────┘
```

---

### 頁面 2: 課表詳情（重點更新）

#### **2A. 簡單訓練（輕鬆跑、長距離跑）**

```
┌─────────────────────┐
│  🟢 輕鬆跑           │
│  今天 11/17         │
├─────────────────────┤
│ 目標                │
│ 📏 8.0 km          │
│ ⏱ 44 分鐘          │
│ 💓 120-145 bpm     │
├─────────────────────┤
│ 訓練說明            │
│ 保持輕鬆配速，      │
│ 感覺舒適為主        │
├─────────────────────┤
│  [ 開始訓練 ]       │
└─────────────────────┘
```

#### **2B. 間歇訓練（關鍵功能）** ⭐

```
┌─────────────────────┐
│  🟠 間歇跑           │
├─────────────────────┤
│ 6 組 × 1000m       │
│                     │
│ 工作段              │
│ 1000m | 4'00"/km   │
│                     │
│ 恢復段              │
│ 400m 慢跑          │
│ 6'00"/km           │
├─────────────────────┤
│ 總距離: 8.4 km     │
│  [ 開始訓練 ]       │
└─────────────────────┘
```

#### **2C. 組合跑（多階段顯示）** ⭐

```
┌─────────────────────┐
│  🟠 組合跑           │
├─────────────────────┤
│ 階段 1: 熱身        │
│ 2.0 km | 6'00"/km  │
│                     │
│ 階段 2: 節奏跑      │
│ 5.0 km | 4'30"/km  │
│                     │
│ 階段 3: 衝刺        │
│ 1.0 km | 3'50"/km  │
│                     │
│ 階段 4: 緩和        │
│ 2.0 km | 6'30"/km  │
├─────────────────────┤
│ 總距離: 10.0 km    │
│  [ 開始訓練 ]       │
└─────────────────────┘
```

**滾動顯示所有階段**，Digital Crown 快速瀏覽。

---

### 頁面 3: 訓練中（核心頁面）

#### **3A. 輕鬆訓練 - 心率模式**

```
┌─────────────────────┐
│     輕鬆跑           │
├─────────────────────┤
│                     │
│       142          │
│      bpm           │
│                     │
├─────────────────────┤
│  Z1 輕鬆  🟢        │
│                     │
│ 120       145       │
│ ━━━|━━●━━━|━━      │
│   目標區間          │
├─────────────────────┤
│ 3.2km │ 17'32" │528│
└─────────────────────┘
```

**心率區間指示器（延伸設計）**：
- 圓點 `●` 顯示當前心率位置
- 區間線延伸到當前值（即使超出範圍）
- 超出範圍時顯示警告

**心率過高示例**：
```
│ 120       145   160 │
│ ━━━|━━━|━━━●━━     │
│   ⚠️ 心率過高       │
```

**心率過低示例**：
```
│ 100   120       145 │
│ ━━●━━|━━━|━━━     │
│   ℹ️ 心率偏低       │
```

---

#### **3B. 配速訓練 - 配速模式**

```
┌─────────────────────┐
│     節奏跑           │
├─────────────────────┤
│                     │
│      4'32"         │
│      /km           │
│                     │
├─────────────────────┤
│  目標 4'30"/km      │
│                     │
│ 4'50"     4'10"     │
│ ━━━|━━●━━|━━       │
│  ±20秒區間          │
├─────────────────────┤
│ 2.8km │ 12'45" │138│
└─────────────────────┘
```

**配速區間指示器（延伸設計）**：
> 配速規則：數字越小=速度越快（右邊），數字越大=速度越慢（左邊）

**配速正常**：
```
│ 4'50"     4'10"     │
│ ━━━|━━●━━|━━       │
│   ✅ 配速理想       │
```

**配速過慢**：
```
│ 5'10" 4'50"  4'10"  │
│ ━●━━|━━━|━━       │
│   ⚠️ 配速過慢       │
```

**配速過快**：
```
│ 4'50"  4'10" 3'50"  │
│ ━━━|━━━|━━━●━     │
│   ⚠️ 配速過快       │
```

---

#### **3C. 間歇訓練 - 分段模式（重點功能）** ⭐⭐⭐

**工作段（衝刺）**：
```
┌─────────────────────┐
│   🔴 工作段 2/6      │
├─────────────────────┤
│                     │
│      3'58"         │
│      /km           │
│                     │
├─────────────────────┤
│  目標 4'00"/km      │
│                     │
│ 4'20"     3'40"     │
│ ━━━|━━●━━|━━       │
├─────────────────────┤
│ 0.82 km  │  3'16"  │
│ 剩餘 180m           │
├─────────────────────┤
│ 下一段: 恢復跑 400m │
└─────────────────────┘
```

**恢復段（慢跑）**：
```
┌─────────────────────┐
│   🟢 恢復段 2/6      │
├─────────────────────┤
│                     │
│      6'12"         │
│      /km           │
│                     │
├─────────────────────┤
│  目標 6'00"/km      │
│  (輕鬆慢跑)         │
├─────────────────────┤
│ 0.25 km  │  1'33"  │
│ 剩餘 150m           │
├─────────────────────┤
│ 下一段: 工作段 1000m│
└─────────────────────┘
```

**恢復段（全休）**：
```
┌─────────────────────┐
│   ⏸️ 全休 2/6        │
├─────────────────────┤
│                     │
│      1'25"         │
│    剩餘時間          │
│                     │
├─────────────────────┤
│  目標 2'00" 休息    │
│                     │
│ ━━━━━━━●━━━        │
│                     │
├─────────────────────┤
│ 心率: 128 bpm       │
│ (正在恢復)          │
├─────────────────────┤
│ 下一段: 工作段 1000m│
└─────────────────────┘
```

---

#### **3D. 組合跑 - 分段模式** ⭐⭐

**當前階段（階段 2/4）**：
```
┌─────────────────────┐
│  階段 2/4: 節奏跑    │
├─────────────────────┤
│                     │
│      4'28"         │
│      /km           │
│                     │
├─────────────────────┤
│  目標 4'30"/km      │
│                     │
│ 4'50"     4'10"     │
│ ━━━|━━●━━|━━       │
├─────────────────────┤
│ 3.8 km  │  17'05"  │
│ 本階段剩餘 1.2km    │
├─────────────────────┤
│ 下一段: 衝刺 1.0km  │
│         3'50"/km    │
└─────────────────────┘
```

---

### 🔊 提示音和震動設計（重點功能）

#### **提示時機**：

| 情況 | 提前時間 | 聲音 | 震動 |
|------|---------|------|------|
| 即將完成當前段 | **5 秒前** | 提示音（短促） | 1 次震動 |
| 進入下一段 | **段開始時** | 提示音（持續） | 2 次震動 |
| 離開目標區間 | **即時** | 警告音 | 持續震動 |
| 返回目標區間 | **即時** | 確認音 | 1 次震動 |
| 完成所有組數 | **最後一組結束** | 完成音（歡快） | 3 次震動 |

#### **實現方式**：
```swift
// 5 秒倒數警告
if remainingDistance <= 50 {  // 假設配速 5'00"/km，5秒約跑50m
    WKInterfaceDevice.current().play(.notification)
    WKInterfaceDevice.current().play(.start)
}

// 進入下一段
func startNextSegment() {
    WKInterfaceDevice.current().play(.directionUp)
    WKInterfaceDevice.current().play(.click)
    WKInterfaceDevice.current().play(.click)
}
```

---

### 🎯 分段邏輯設計（核心算法）

#### **間歇訓練數據結構**：
```swift
struct IntervalWorkout {
    let repeats: Int                    // 6 組
    let workSegment: WorkoutSegment     // 工作段: 1000m, 4'00"/km
    let recoverySegment: WorkoutSegment // 恢復段: 400m 或 2'00" 休息

    var currentLap: Int = 1
    var currentPhase: Phase = .work     // .work 或 .recovery
}

enum RecoveryType {
    case activeRecovery(distance: Double, pace: String)  // 恢復跑
    case rest(duration: TimeInterval)                    // 全休
}
```

#### **組合跑數據結構**：
```swift
struct CombinationWorkout {
    let segments: [ProgressionSegment]

    var currentSegmentIndex: Int = 0
}

struct ProgressionSegment {
    let name: String          // "熱身", "節奏跑", "衝刺"
    let distanceKm: Double
    let targetPace: String
    let description: String?
}
```

#### **實時追蹤邏輯**：

**間歇訓練**：
```swift
func updateIntervalProgress(currentDistance: Double) {
    let segmentDistance = currentPhase == .work
        ? workSegment.distance
        : recoverySegment.distance

    let progressInSegment = currentDistance - segmentStartDistance
    let remainingInSegment = segmentDistance - progressInSegment

    // 5 秒倒數警告（假設配速）
    let estimatedSecondsRemaining = remainingInSegment / currentSpeed
    if estimatedSecondsRemaining <= 5 && !hasPlayedWarning {
        playWarningSound()
        hasPlayedWarning = true
    }

    // 完成當前段
    if progressInSegment >= segmentDistance {
        completeCurrentSegment()
        startNextSegment()
    }
}

func completeCurrentSegment() {
    if currentPhase == .work {
        currentPhase = .recovery
    } else {
        currentPhase = .work
        currentLap += 1
    }
    segmentStartDistance = currentDistance
    hasPlayedWarning = false
}
```

**組合跑**：
```swift
func updateCombinationProgress(currentDistance: Double) {
    let currentSegment = segments[currentSegmentIndex]
    let progressInSegment = currentDistance - segmentStartDistance
    let remainingInSegment = currentSegment.distanceKm * 1000 - progressInSegment

    // 5 秒倒數
    let estimatedSecondsRemaining = remainingInSegment / currentSpeed
    if estimatedSecondsRemaining <= 5 && !hasPlayedWarning {
        playWarningSound()
        hasPlayedWarning = true
    }

    // 完成當前段
    if progressInSegment >= currentSegment.distanceKm * 1000 {
        currentSegmentIndex += 1
        segmentStartDistance = currentDistance
        hasPlayedWarning = false
        playNextSegmentSound()
    }
}
```

---

### 頁面 4: 訓練完成

```
┌─────────────────────┐
│  ✅ 訓練完成         │
├─────────────────────┤
│   間歇跑 6×1000m    │
│                     │
│  8.4 km             │
│  36'42"             │
├─────────────────────┤
│ 平均配速 4'22"/km   │
│ 平均心率 162 bpm    │
│ 最大心率 178 bpm    │
│ 卡路里  620 kcal    │
├─────────────────────┤
│ 完成 6/6 組         │
│ 工作段平均 3'58"/km │
├─────────────────────┤
│  [ 完成 ]           │
└─────────────────────┘
```

**行為**：
- 用戶點擊「結束訓練」按鈕才停止
- 顯示詳細總結（包含分段統計）
- 用戶點擊「完成」才返回課表列表
- 數據自動保存到 HealthKit

---

## 🏗️ 技術實現方案

### 架構圖

```
┌─────────────────────────────────────┐
│         iOS App (Paceriz)           │
│  ┌─────────────────────────────┐   │
│  │  WatchConnectivityManager   │   │
│  │  - 自動同步課表              │   │
│  │  - 用戶設置同步偏好          │   │
│  │  - 課表變更時主動推送        │   │
│  └─────────────────────────────┘   │
└─────────────┬───────────────────────┘
              │ WatchConnectivity
              │ + App Group
              ↓
┌─────────────────────────────────────┐
│       watchOS App (Paceriz)         │
│  ┌─────────────────────────────┐   │
│  │  WatchDataManager           │   │
│  │  - 接收課表數據              │   │
│  │  - 本地緩存                  │   │
│  │  - 手動觸發同步              │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  WorkoutManager             │   │
│  │  - 訓練會話管理              │   │
│  │  - 實時數據追蹤              │   │
│  │  - 分段邏輯處理              │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  SegmentTracker             │   │
│  │  - 間歇訓練分段              │   │
│  │  - 組合跑分段                │   │
│  │  - 提示音觸發                │   │
│  └─────────────────────────────┘   │
└─────────────┬───────────────────────┘
              │ HealthKit
              ↓
┌─────────────────────────────────────┐
│         Apple Health                │
│  - 自動保存訓練記錄              │   │
│  - 心率、GPS、距離數據            │   │
└─────────────┬───────────────────────┘
              │ 自動同步
              ↓
┌─────────────────────────────────────┐
│         iOS App (Paceriz)           │
│  - 自動讀取 HealthKit 數據          │
│  - 上傳到後端 API                   │
└─────────────────────────────────────┘
```

---

### 數據同步策略（確認版）

#### **iOS App 同步設置**：

```swift
// 在 iOS App 設置頁新增選項
struct WatchSyncSettings {
    var autoSync: Bool = true           // 自動同步開關
    var syncOnPlanChange: Bool = true   // 課表變更時同步
    var syncInterval: TimeInterval = 3600  // 自動同步間隔（1小時）
}
```

**自動同步時機**：
1. iOS app 啟動時
2. 用戶生成/修改課表後
3. 定期背景同步（可選，用戶設置）

**手動同步**：
- watchOS app 提供「刷新」按鈕
- 下拉刷新手勢

#### **同步內容**：
```json
{
  "weeklyPlan": {
    "id": "plan_123_week_3",
    "weekOfPlan": 3,
    "totalWeeks": 12,
    "days": [
      {
        "dayIndex": "2025-11-17",
        "trainingType": "interval",
        "dayTarget": "間歇跑 6×1000m",
        "trainingDetails": {
          "repeats": 6,
          "work": {
            "distanceM": 1000,
            "pace": "4:00"
          },
          "recovery": {
            "distanceM": 400,
            "pace": "6:00"
          }
        }
      }
    ]
  },
  "userProfile": {
    "maxHR": 190,
    "restingHR": 55,
    "vdot": 48.5
  },
  "heartRateZones": [...]
}
```

---

### SegmentTracker 核心邏輯

```swift
class SegmentTracker: ObservableObject {
    enum WorkoutType {
        case simple              // 輕鬆跑、長距離
        case interval            // 間歇跑
        case combination         // 組合跑
    }

    enum IntervalPhase {
        case work               // 工作段
        case recovery           // 恢復段（跑步）
        case rest               // 恢復段（全休）
    }

    // 當前狀態
    @Published var currentSegmentIndex: Int = 0
    @Published var currentLap: Int = 1
    @Published var currentPhase: IntervalPhase = .work

    // 距離追蹤
    private var segmentStartDistance: Double = 0
    private var hasPlayedWarning: Bool = false

    // 計算當前段的進度
    func updateProgress(currentDistance: Double, currentSpeed: Double) {
        switch workoutType {
        case .interval:
            updateIntervalProgress(currentDistance, currentSpeed)
        case .combination:
            updateCombinationProgress(currentDistance, currentSpeed)
        case .simple:
            break  // 簡單訓練無需分段追蹤
        }
    }

    private func updateIntervalProgress(_ distance: Double, _ speed: Double) {
        let segmentDistance = currentSegmentDistance()
        let progress = distance - segmentStartDistance
        let remaining = segmentDistance - progress

        // 5 秒倒數警告
        if speed > 0 {
            let estimatedSeconds = remaining / speed
            if estimatedSeconds <= 5 && estimatedSeconds > 0 && !hasPlayedWarning {
                triggerWarning()
                hasPlayedWarning = true
            }
        }

        // 完成當前段
        if progress >= segmentDistance {
            completeSegment()
        }
    }

    private func completeSegment() {
        playCompletionSound()

        if currentPhase == .work {
            currentPhase = recoveryType() == .rest ? .rest : .recovery
        } else {
            currentPhase = .work
            currentLap += 1
        }

        segmentStartDistance = currentDistance
        hasPlayedWarning = false
        playNextSegmentSound()
    }

    private func triggerWarning() {
        WKInterfaceDevice.current().play(.notification)
        WKInterfaceDevice.current().play(.start)
    }

    private func playNextSegmentSound() {
        WKInterfaceDevice.current().play(.directionUp)
        WKInterfaceDevice.current().play(.click)
        WKInterfaceDevice.current().play(.click)
    }
}
```

---

## 🎨 UI 色系設計

### 顏色定義（SwiftUI）

```swift
extension Color {
    // 主色調
    static let paceriPrimary = Color(hex: "1E90FF")
    static let pacerizSecondary = Color(hex: "00BFFF")
    static let pacerizBackground = Color(hex: "1C1C1E")
    static let pacerizSurface = Color(hex: "2C2C2E")

    // 心率區間
    static let zoneEasy = Color(hex: "4CAF50")      // Z1 綠色
    static let zoneMarathon = Color(hex: "2196F3")  // Z2 藍色
    static let zoneThreshold = Color(hex: "FFC107") // Z3 黃色
    static let zoneAerobic = Color(hex: "FF9800")   // Z4 橙色
    static let zoneInterval = Color(hex: "F44336")  // Z5 紅色

    // 訓練類型
    static let typeEasy = Color.green
    static let typeInterval = Color.orange
    static let typeLong = Color.blue
    static let typeRace = Color.red
    static let typeRest = Color.gray
}
```

---

## 📋 Phase 1 功能清單

### ✅ 必須實現

**數據層**：
- [x] 共享數據模型 (Shared Framework)
- [x] WatchConnectivity 服務（iOS）
- [x] WatchDataManager（watchOS）
- [x] App Group 本地緩存

**UI 層（watchOS）**：
- [x] 課表列表頁
- [x] 課表詳情頁（含組合跑多段顯示）
- [x] 訓練中頁面（心率/配速延伸區間）
- [x] 訓練完成總結頁

**訓練邏輯**：
- [x] WorkoutManager（HealthKit 集成）
- [x] SegmentTracker（間歇/組合分段追蹤）
- [x] 提示音和震動系統
- [x] 實時心率/配速監控
- [x] GPS 軌跡記錄

**同步功能**：
- [x] iOS 自動同步設置
- [x] watchOS 手動刷新
- [x] 離線緩存支持

**支持訓練類型**（跑步相關）：
- [x] 輕鬆跑、恢復跑、LSD
- [x] 間歇跑（重點）
- [x] 組合跑（重點）
- [x] 節奏跑、閾值跑
- [x] 長距離跑
- [x] 休息日

---

### ❌ Phase 2 延後

- 力量訓練、瑜伽、騎行
- 語音播報
- 歷史訓練記錄查看
- 複雜的自定義調整
- 社交功能

---

## 🚀 開發計劃

### Week 1: 基礎架構（5-6 天）
- Day 1-2: 創建 watchOS target + Shared framework
- Day 3: 實現 WatchConnectivity（iOS 端）
- Day 4: 實現 WatchDataManager（watchOS 端）
- Day 5: 課表列表和詳情頁 UI
- Day 6: 測試同步功能

### Week 2: 訓練核心（6-7 天）
- Day 7-8: WorkoutManager + HealthKit 集成
- Day 9-10: SegmentTracker 實現（間歇/組合邏輯）
- Day 11: 訓練中 UI（心率/配速延伸區間）
- Day 12: 提示音和震動系統
- Day 13: 訓練完成總結頁

### Week 3: 優化和測試（3-4 天）
- Day 14: UI 色系優化
- Day 15: 邊緣案例處理（無網路、GPS 弱等）
- Day 16-17: 完整測試和調試

**總計：14-17 天**

---

## ✅ 確認事項

以下設計已根據你的反饋確認：

1. ✅ **組合跑詳情頁** - 顯示所有階段，可滾動查看
2. ✅ **訓練中區間延伸** - 心率和配速指示器延伸到當前值
3. ✅ **緊湊型指標顯示** - 單頁顯示所有信息
4. ✅ **配速區間** - 統一 ±20 秒
5. ✅ **間歇/組合分段追蹤** - 核心功能，含提示音
6. ✅ **只支持跑步** - Phase 1 專注跑步訓練
7. ✅ **簡短區間命名** - 支持多語言
8. ✅ **手動結束訓練** - 顯示總結後用戶點擊完成
9. ✅ **可選自動同步** - iOS 設置中配置
10. ✅ **watchOS 9.0+** - 最低系統版本

---

## 🎯 下一步

請確認以上設計無誤後，我將立即開始實施：

1. 創建 watchOS target 和 Shared framework
2. 實現數據同步邏輯
3. 開發 UI 頁面
4. 實現核心訓練追蹤功能

準備好了嗎？🚀
