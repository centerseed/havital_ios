{
  "target_internal": "string", // 對目標的內部評估與分析
  "target_evaluate": "string", // 對目標的評估與分析
  "total_weeks": "number", // 總訓練週數
  "training_hightlight": "string", // 訓練計畫的亮點與重點描述
  "training_plan_name": "string", // 訓練計畫名稱
  "training_plan_overview": ["string"], // 訓練計畫的概述
  "training_stage_discription": [ // 訓練階段的描述列表
    {
      "stage_name": "string", // 階段名稱
      "stage_description": "string", // 階段描述
      "week_start": "number", // 階段開始週數
      "week_end": "number" // 階段結束週數（可選）
    }
  ],
  "user_information": { // 使用者信息
    "age": "number", // 年齡
    "workout_days": "number", // 每週可訓練天數
    "current_distance": "number", // 當前的跑步距離（公里）（可選）
    "current_pace": "string", // 當前的配速（格式：秒/公里）（可選）
    "target_distance": "number", // 目標跑步距離（公里）（可選）
    "target_pace": "string", // 目標配速（格式：秒/公里）（可選）
    "proposed_vdot": "number" // 目標的VDOT
  }
}
